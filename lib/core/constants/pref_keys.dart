part of '_constants.dart';

class PreferencesKeys {
  static const authToken       = 'x-auth-token';      // ← access
  static const refreshTokenKey = 'x-refresh-token';   // ← new
  static const apiKey          = 'x-api-key';
}